@model ActivityServiceModel 

<div class="col-md-4"> 
    <div class="card mb-3"> 
        <img class="card-img-top" src="@Model.ImageUrl" alt="Activity Image"> 
        <div class="card-body text-center"> 
            <h4>@Model.Title</h4> 
            <h6>Контакти: <b>@Model.Contact</b></h6> 
            <h6>Описание: <b>@Model.Description</b></h6>
            <br /> 
            <a asp-controller="Activity" asp-action="Details" asp-route-id="@Model.Id" class="btn btn-success">Детайли</a>
            @if (this.User.Identity.IsAuthenticated) 
            { 
                <a asp-controller="Activity" asp-action="Edit" asp-route-id="@Model.Id" class="btn btn-warning">Промени</a>
                <a asp-controller="Activity" asp-action="Delete" asp-route-id="@Model.Id" class="btn btn-danger">Изтрий</a> 
            } 
        </div> 
    </div> 
</div>